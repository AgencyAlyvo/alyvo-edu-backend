/**
 * Corps JSON pour l'upload des captures myBC (PNG en base64).
 */
export type UploadMybcScreenshotsPayload = {
  studentHomeBase64: string
  prospectMenuBase64: string
  registrationStatusBase64: string
}
